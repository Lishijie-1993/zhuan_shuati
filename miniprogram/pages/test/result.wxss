page {
  background-color: #f7f8fa;
  height: 100vh;
}

.container {
  min-height: 100vh;
  background-color: #f7f8fa;
  display: flex;
  flex-direction: column;
}

/* 顶部导航栏 */
.nav-bar {
  display: flex;
  align-items: center;
  height: 88rpx;
  padding: 0 30rpx;
  background-color: #fff;
  border-bottom: 1rpx solid #f2f2f2;
  flex-shrink: 0;
}

.nav-left {
  width: 60rpx;
  height: 88rpx;
  display: flex;
  align-items: center;
  justify-content: flex-start;
}

.nav-back-icon {
  font-size: 48rpx;
  color: #333;
  font-weight: bold;
}

.nav-title {
  flex: 1;
  text-align: center;
  font-size: 34rpx;
  font-weight: bold;
  color: #333;
}

.nav-placeholder {
  width: 60rpx;
}

/* 内容区域 */
.content-area {
  flex: 1;
  padding: 30rpx;
}

.result-card {
  background: #fff;
  border-radius: 24rpx;
  padding: 60rpx 40rpx;
  box-shadow: 0 8rpx 30rpx rgba(0,0,0,0.08);
  margin-bottom: 40rpx;
}

.result-header {
  text-align: center;
  padding-bottom: 40rpx;
  border-bottom: 1rpx solid #f0f0f0;
}

.result-label {
  font-size: 28rpx;
  color: #999;
  display: block;
  margin-bottom: 30rpx;
}

.score-circle {
  width: 200rpx;
  height: 200rpx;
  border-radius: 50%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  margin: 0 auto 30rpx;
}

.score-circle.pass {
  background: linear-gradient(135deg, #07c160 0%, #5fc87a 100%);
}

.score-circle.fail {
  background: linear-gradient(135deg, #fa5151 0%, #ff7875 100%);
}

.score-num {
  font-size: 72rpx;
  font-weight: bold;
  color: #fff;
}

.score-unit {
  font-size: 28rpx;
  color: rgba(255,255,255,0.8);
}

.pass-status {
  font-size: 32rpx;
  font-weight: bold;
  padding: 10rpx 30rpx;
  border-radius: 30rpx;
  display: inline-block;
}

.pass-status.pass {
  background: rgba(7, 193, 96, 0.1);
  color: #07c160;
}

.pass-status.fail {
  background: rgba(250, 81, 81, 0.1);
  color: #fa5151;
}

.result-detail {
  padding-top: 40rpx;
}

.detail-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20rpx 0;
}

.detail-label {
  font-size: 30rpx;
  color: #666;
}

.detail-value {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
}

.detail-value.correct {
  color: #07c160;
}

.actions {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

.btn-action {
  height: 88rpx;
  line-height: 88rpx;
  border-radius: 44rpx;
  background: #2D5CF6;
  color: #fff;
  font-size: 32rpx;
  font-weight: bold;
}

.btn-action.secondary {
  background: #f8f8f8;
  color: #666;
}
